/**
 * 아래 함수를 화살표 함수로 바꾸세요.
 * - 함수 이름은 변경하지 마세요.
 * - export const testFuncs는 변경하지 마세요.
 */

const addNumbers = (num1, num2, num3) => num1 + num2 + num3;
const concatStrings = (str1, str2, str3) => str1.concat(str2).concat(str3);

export const testFuncs = {
  addNumbers,
  concatStrings,
};
